# Design handoff bundle (source only — not part of the deployed site)
_design_src/
design.tar.gz
*.tar.gz

# Node / Tailwind build tooling (css/tailwind.css IS committed; deps are not)
node_modules/

# Local agent / editor config (not part of the published site)
.claude/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.log
.idea/
.vscode/

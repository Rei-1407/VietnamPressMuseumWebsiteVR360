# Design handoff bundle + redesign reference/captures (not part of the site)
_design_src/
_design_ref/
design.tar.gz
*.tar.gz

# Node / Tailwind build tooling (css/tailwind.css IS committed; deps are not)
node_modules/

# Local agent / editor config (not part of the published site)
.claude/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.log
.idea/
.vscode/

# font sources (chỉ deploy bản woff2 đã nén)
font/*.ttf
font/*.otf

# thư mục sao lưu tạm của người dùng
_backup_*/
